# Kindred

A three-screen Flutter membership app for a neighborhood reading circle.

Home introduces the club, the form collects a new member with validation, and the detail screen issues a membership card. Navigation uses named routes: `/`, `/register`, and `/detail`.

## Run

```bash
flutter run
```
